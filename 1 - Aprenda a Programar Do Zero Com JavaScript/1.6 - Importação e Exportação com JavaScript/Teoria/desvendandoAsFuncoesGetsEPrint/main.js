// Aula 2 - Criando a Fimção Gets e Print

    // const funcoes = require('./funcoes-auxiliares');
    // console.log(funcoes.gets());

// Aula 3 - O que é Object Destructuring

    const pessoa = {nome: 'Giovana'};

    // Destructuring o objeto
        const { nome } = pessoa;

    // const {gets, print}= require('./funcoes-auxiliares');
    // print(gets());

// Aula 4 - Considerações Finais da Aula